# Build Feasibility And Cost Report Template

This is now a legacy alias for the newer `Build MVP` structure.

Use [build-mvp-template.md](build-mvp-template.md) as the primary
template for the third default output.

Compatibility rule:

- If an older prompt explicitly asks for `Build Feasibility And Cost`,
  still use the `Build MVP` structure.
- Keep the cost-research subsections inside the `Cost Analysis` section.
- Preserve the old wording only when the user explicitly wants the legacy title.
